# Host-side connection to the root superproject's compose Postgres
# (`docker compose up -d postgres` from /Users/ash/code/mboss).
# From inside the compose network the host is `postgres` instead of `localhost`.
DATABASE_URL="postgres://postgres:mboss@localhost:5432/mboss"
